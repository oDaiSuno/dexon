# Dexon 设计系统 · Vercel A

> **人格决策**（2026-09-03，用户在三个真实组件渲染方向中确认）：Vercel 经典——Geist 字体、单色灰阶 + 蓝点缀、黑/白反转主按钮、语义色只表达状态、明暗双主题。
>
> **令牌唯一来源**：`src/renderer/globals.css`（应用级）与 `src/renderer/chat.css`（聊天区，别名到灰阶坡）。本文档是设计决策的记录，改值先改令牌文件，再同步本文。

---

## 1. 设计原则（按优先级）

1. **事实保真**：状态、数字、单位、限定条件不可为视觉让路；不确定的状态不装饰成确定。
2. **层级先行**：先用排版（字号、字重、间距、密度）建立层级， surfaces 和颜色只处理排版处理不了的事。
3. **单色克制**：界面主体是灰阶；颜色只在表达**状态、选中、焦点、数据**时出现，且必须伴随非色彩线索（文字、图标、位置）。
4. **平面无饰**：拒绝渐变、光晕、纹理、玻璃、装饰阴影；画布是纯色平面，分隔靠 hairline 和间距。
5. **安静而精确**：动效只解释状态变化（见 §6），不用动效制造性。

## 2. 色板

### 2.1 灰阶坡（主题自适应的值域，组件不直接消费）

| 档位          | 浅色      | 深色      | 语义                       |
| ------------- | --------- | --------- | -------------------------- |
| `--gray-0`    | `#ffffff` | `#0f0f0f` | 面板表面（`--bg-panel`）   |
| `--gray-50`   | `#fafafa` | `#0a0a0a` | 页面画布（`--bg`）         |
| `--gray-100`  | `#f2f2f2` | `#1c1c1c` | 悬停                       |
| `--gray-200`  | `#eaeaea` | `#262626` | 边线（浅）/ 选中底         |
| `--gray-300`  | `#d4d4d4` | `#333333` | 强边线（浅）/ 边线（深）   |
| `--gray-400`  | `#a1a1a1` | `#444444` | 弱文字（浅）/ 强边线（深） |
| `--gray-500`  | `#8f8f8f` | `#707070` | 占位、弱文字（深）         |
| `--gray-600`  | `#666666` | `#a1a1a1` | 次要文字                   |
| `--gray-800`  | `#333333` | `#3d3d3d` | 实底块边线                 |
| `--gray-900`  | `#171717` | `#e5e5e5` | 主按钮悬停（浅）           |
| `--gray-1000` | `#0a0a0a` | `#ededed` | 主文字 / 主按钮底（浅）    |

### 2.2 语义变量（组件唯一允许消费的层）

| 用途           | 变量                                                                                                                       |
| -------------- | -------------------------------------------------------------------------------------------------------------------------- |
| 表面           | `--bg` `--bg-panel` `--bg-hover` `--bg-selected` `--bg-subtle`                                                             |
| 文字           | `--text` `--text-muted` `--text-dim`                                                                                       |
| 边线           | `--border` `--border-strong`                                                                                               |
| 选中/焦点/链接 | `--accent` `--accent-hover` `--accent-soft` `--accent-soft-border` `--accent-contrast` `--accent-chip-bg/fg` `--selection` |
| 主按钮         | `--btn-bg` `--btn-bg-hover` `--btn-fg`                                                                                     |
| 状态           | `--success` `--danger` `--warning` `--info`（各带 `-soft`/`-border`）                                                      |
| 反白           | `--on-accent` `--on-danger` `--user-bg/fg` `--tool-bg/fg/border` `--assistant-bg`                                          |
| 数据           | `--heat-1..6`（推理等级等 6 档热力标尺，由 warning/danger 锚点 `color-mix` 派生）                                          |

### 2.3 蓝的使用边界

蓝（浅色 `#0060dd`，4.97:1 AA；深色 `#3291ff`）**只**出现在：

- 焦点环与输入框聚焦描边
- 列表/分段控件的选中态
- 链接、可点文字
- 「信息」类徽章（如未读计数，配数字本身作非色彩线索）

**禁止**：把推荐值、成功状态、成本分量染成蓝色或绿色；蓝不是装饰色。

### 2.4 主按钮：黑/白反转

- 浅色：黑底（`--btn-bg` = `#0a0a0a`）白字；悬停加深一档。
- 深色：白底（`#ededed`）黑字；悬停提亮到纯白。
- 幽灵按钮：透明底 + `--border-strong` 边线 + `--text` 字。
- 危险实底按钮：`--danger` 底 + `--on-danger` 字（双主题各自保证 AA）。

## 3. 字体

### 3.1 字体栈

- 无衬线：`Geist` 打头（本地打包 `src/renderer/fonts/GeistSans.woff2`，OFL 1.1），中文回退 PingFang SC / Microsoft YaHei / Noto Sans CJK SC。
- 等宽：`Geist Mono` 打头（`GeistMono.woff2`）回退系统等宽。
- 字体随包分发、零 CDN（`font-src 'self' app:`）；共享常量在 `src/shared/font-stack.ts`（主进程错误页、mermaid、docx 预览共用）。
- **Mono 只用于**：代码、命令、路径、模型名、区域/计划 ID、时间戳、token 数、耗时——不用于整句正文。

### 3.2 字阶（UI 唯一允许的字号，聊天区除外）

| 令牌                | 值             | 用途                                     |
| ------------------- | -------------- | ---------------------------------------- |
| `--text-display`    | 20px / 600     | 单个页面级陈述（如欢迎语），一屏至多一处 |
| `--text-heading-lg` | 18px / 600     | 面板内大节标题                           |
| `--text-title`      | 16px / 600     | 屏幕标题、弹窗标题                       |
| `--text-heading`    | 14px / 600     | 小节标题                                 |
| `--text-body-lg`    | 14px / 400–500 | 强调正文、列表行                         |
| `--text-body`       | 13px / 400     | 阅读、控件、正文默认                     |
| `--text-label`      | 12px / 500     | 紧凑标签、按钮文字                       |
| `--text-meta`       | 11px / 400     | 元信息、时间戳                           |
| `--text-micro`      | 10px / 400–700 | 高密度日志、角标                         |

字重只有三档：`--weight-regular` 400 / `--weight-medium` 500 / `--weight-semibold` 600。同级内容必须同字阶同字重；不因文字更长而缩号。

### 3.3 聊天区字号

聊天排版走 `--chat-f-*`（chip 11.5 / micro 11 / name 12.5 / meta 12 / body 13.5）乘以 `--chat-font-scale`（设里四档缩放），组件内经 `scaledChatFont()` 消费——聊天字阶独立于 UI 字阶，是既定体系。

## 4. 间距与密度

- 4px 基档：`--space-1..12` = 4/8/12/16/20/24/32/40/48。
- 允许 ≤2px 的光学微调；**其余一律落档**（原 5/6/7/9/13px 等拍脑袋值已清零）。
- 关系优先于均一栈：标题→首段贴近；组内 `--space-1..2`；组间 `--space-3..4`；大节转折 `--space-6..8`。

## 5. 圆角与阴影

| 档                 | 值    | 用途                     |
| ------------------ | ----- | ------------------------ |
| `--radius-chip`    | 4px   | 参数芯片、复选框、小徽章 |
| `--radius-control` | 6px   | 按钮、输入框、菜单项     |
| `--radius-card`    | 8px   | 卡片、弹窗容器、代码块   |
| `--radius-pill`    | 999px | 胶囊、进度轨、开关       |

特例（3 个，记录在案）：圆形 `50%`；用户气泡尾角 `"12px 12px 4px 12px"`；直角 `0`。
聊天区维持同心圆角：chip 6 / control 8 / card 10（`--bui-r-*`）。
阴影只有一处命名档 `--shadow-popover`（浮层用）；装饰性阴影禁止。

## 6. 动效（transitions.dev，五维令牌）

动效是**功能性资产**，本轮换皮不换动效；全部时长/缓动走 `chat.css` 顶部令牌：

- 时长：stagger 40 / micro 80 / quick 150 / fast 250 / medium 350 / slow 400 / very-slow 500ms
- 缓动：smooth-out（`cubic-bezier(0.23,1,0.32,1)`）为主，bounce 用于对勾弹入
- 既有时刻（保持不变）：手风琴 0fr↔1fr、spinner→对勾变形、思考 shimmer、流式文字词级浮现、欢迎页交错入场
- 所有动效必须带 `prefers-reduced-motion` 降级；禁止滚动触发整页 reveal、跑马灯、装饰脉冲

## 7. 组件规范摘要

| 组件             | 规范                                                                                                                   |
| ---------------- | ---------------------------------------------------------------------------------------------------------------------- |
| 会话侧栏         | 选中项 `--bg-selected` + `--text`；悬停 `--bg-hover`；未读 chip 蓝 tint                                                |
| 用户气泡（本地） | `--bui-bubble`（gray-200）+ 1px 描边，尾角 4px；渠道气泡保留品牌色白字（微信/Telegram/飞书品牌色是唯一记录内色彩例外） |
| 助手正文         | 无卡片直排，行高 1.7，mono 行内码 `--bg-subtle` 底                                                                     |
| 工具调用行       | 状态图标 + 动词标签 + mono 参数芯片 + 等宽数字耗时；完成态不自动回退                                                   |
| 表格/日志        | 文本列左对齐、数值列右对齐（含表头）；`tabular-nums`；语义色只标状态词                                                 |
| 画布             | 纯色平面（聊天区白/纯黑），无纹理无渐变                                                                                |

## 8. 拒绝清单

- 全大写眉题、装饰性编号、徽章胶囊滥用（普通元信息不加胶囊）
- 渐变、光晕、玻璃、纸纹、网格底纹、彩色侧轨
- 到处包卡片、卡片套卡片；用边框修补弱层级
- 任意字号、为长文缩号、居中对齐的表单标签列
- 无数据的装饰图表、无含义的颜色、图标瓷砖
- 缩到难读的小灰字来塞密度——先改文案再改排版

## 9. 实现与守门

- **消费方式**：组件内联样式引用 `var(--*)`；Tailwind `@theme` 映射少量类；聊天组件经 `--bui-*` 别名（值 = 灰阶坡），聊天画布纹理已移除。
- **新增值的规则**：新颜色先进灰阶坡/语义层，新字号/圆角/间距必须有令牌；组件内出现裸 hex、裸字号、裸圆角即违约。
- **契约测试**：`theme-tokens.test.mjs`、`semantic-colors.test.mjs`（AA 对比度、语义变量覆盖）、`font-stack.test.mjs`（字体栈一致性）、`offline-assets.test.mjs`（零 CDN）。改令牌先跑这些测试。
- **视觉验证**：重大视觉变更用真实组件渲染预览 + 明暗双主题实机截图核对，再全量实施。
